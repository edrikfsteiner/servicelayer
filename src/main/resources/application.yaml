spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://edrikfsteiner_db_user:YZ5mdhoAPtQSpaPh@cluster.5tnym0d.mongodb.net/ingestion_security?appName=Cluster}

  threads:
    virtual:
      enabled: true

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin123

app:
  security:
    jwt:
      secret: ${JWT_SECRET}
      expiration-time: 3600
  messaging:
    exchange: ingestion.exchange
    queue-main: lakehouse.bronze.queue
    queue-dlq: lakehouse.bronze.dlq
    routing-key-main: ingestion.routing.key
    routing-key-dlq: ingestion.dlq.routing.key

rate-limit:
  capacity: 1000   # max requests per window per tenant
  minutes: 1       # sliding window size in minutes


silver:
  scheduler:
    cron: "0 0 0 * * *"  # todo dia à meia-noite; override para testes ex: "0 * * * * *" (a cada minuto)